% 
% DG
% 需要在切片的时候进行session内平均
% ssvep_b：
%   session：MGv
%   session内repeat：50（13session）
%   session包括：MGnv，SG，SSGnv（center），SSGv
%   session内repeat：14（61session）


% QQ_new
% ssvep_b：
%   session包括MGv，MGnv，SG，SSGnv（center），SSGv
%   session内repeat：3（150session，由于session的缺失，导致有些没有）